# ──────────────────────────────────────────────────────────────────────
#  venv_lmdeploy — chỉ chứa lmdeploy + torch 2.4 cu121 (combo stable).
#
#  Job duy nhất: chạy `lmdeploy serve api_server` cho VieNeu-TTS-v2 LM
#  backbone (bfloat16, FlashAttention, paged KV cache) trên :23333.
#  Expose OpenAI-compatible /v1/chat/completions → vieneu_server (venv_vieneu)
#  call vào để gen audio tokens.
#
#  TUYỆT ĐỐI KHÔNG cài vieneu/neucodec/transformers mới vào đây — sẽ kéo
#  torch upgrade phá ABI của lmdeploy. venv này tách hẳn cho lmdeploy.
#
#  Torch 2.4 cu121 install riêng bằng setup.sh từ pytorch index, KHÔNG
#  trong file này.
# ──────────────────────────────────────────────────────────────────────

lmdeploy==0.9.0
# lmdeploy 0.9.0 = bản đầu tiên có Qwen3ForCausalLM rewrite trong pytorch engine.
# Đã bỏ dependency LogitsWarper. Hỗ trợ qwen3 (cần cho VieNeu-TTS-v2).
# torch 2.4 cu121 đồng bộ với xgrammar/tvm_ffi wheels của 0.9 (cùng torch 2.4 ABI).

# transformers 4.51+ cần thiết để nhận diện qwen3 architecture.
# 4.57.6 là bản latest 4.x ổn định.
transformers>=4.51,<5.0
huggingface-hub<1.0

# lmdeploy 0.7.2 known bug: thiếu khai báo trong deps → add tay.
partial_json_parser
