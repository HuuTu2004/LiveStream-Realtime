# ──────────────────────────────────────────────────────────────────────
#  venv_vieneu — FAST mode (per VieNeu official docs)
#  https://docs.vieneu.io/vi/docs/getting-started/installation
#  https://docs.vieneu.io/vi/docs/sdk/fast-mode
#
#  Một lệnh `pip install vieneu[gpu]` kéo theo:
#    - torch (compatible version do vieneu pin)
#    - lmdeploy turbomind (cho in-process backbone inference)
#    - neucodec (codec lib + ONNX runtime)
#    - transformers, tokenizers, aiohttp, requests
#
#  FAST mode = vieneu_server tự load Qwen3 backbone in-process bằng LMDeploy
#  turbomind. KHÔNG cần lmdeploy api_server riêng → ít VRAM contention, ít
#  HTTP roundtrip, latency thấp hơn ~30%.
#
#  Server-side (vieneu_server.py):
#    1. POST /infer_stream với text
#    2. Split sentences regex
#    3. tts.infer() batch mỗi câu (mode='fast' → in-process turbomind gen
#       audio_tokens; ONNX codec decode tokens → PCM)
#    4. Stream HTTP response chunks 200ms PCM 24kHz
# ──────────────────────────────────────────────────────────────────────

vieneu[gpu]
aiohttp>=3.9
numpy>=2.0
soundfile>=0.12

# CUDA codec decoder → audio gen ~1.78x realtime (vs CPU 1.0x).
# Đánh đổi: thêm GPU compute (codec dùng SM) → musetalk fps drop nhẹ trên
# 3090 đã sát limit. Nhưng audio responsive hơn → livestream UX tốt hơn.
# Default GPU; revert CPU (onnxruntime) chỉ nếu GPU VRAM cực thiếu.
onnxruntime-gpu>=1.20
