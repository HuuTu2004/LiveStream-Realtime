# ════════════════════════════════════════════════════════════════════════
#  LiveTalking Sales — Tất cả dependency trong 1 file duy nhất
#  Cài đặt: pip install -r requirements.txt
#  Yêu cầu: Python 3.10+, CUDA 12.1 (PyTorch GPU), ffmpeg system
# ════════════════════════════════════════════════════════════════════════

# ─── HTTP server + async ──────────────────────────────────────────────
aiohttp>=3.9.0
aiohttp_cors
# Transport = wsstream (MPEG-TS over WebSocket + JSMpeg trong browser).
# WebRTC/aiortc đã loại bỏ — server chỉ serve TCP, bypass NAT trên Vast.AI.

# ─── LLM brain (async OpenAI / Ollama / vLLM / Vast.AI) ───────────────
openai>=1.40.0
httpx>=0.27.0

# ─── TikTok Live scraper (comment/like/join/gift) ────────────────────
TikTokLive>=6.0.0

# ─── TTS engine (Vietnamese, voice cloning) ─────────────────────────
# VieNeu-TTS — Apache 2.0, voice clone 3-5s.
# Modes: standard (GGUF+ONNX) | turbo (0.3B faster) | gpu (cần lmdeploy) | remote
vieneu>=1.1.0
# LMDeploy KHÔNG include trong core deps vì:
#   - lmdeploy[all] force torch<=2.10 → conflict với torch 2.11+cu128 (Blackwell)
#   - lmdeploy chưa stable trên Blackwell sm_120
# Nếu muốn gpu mode, cài riêng sau:  pip install lmdeploy
# Default vieneu_mode = turbo (CPU/GPU, không cần lmdeploy)

# ─── Avatar models (MuseTalk diffusion + Wav2Lip + Ultralight) ───────
transformers==4.46.2
diffusers
accelerate
omegaconf
einops
lpips==0.1.3

# ─── Audio processing ─────────────────────────────────────────────────
soundfile==0.12.1
librosa
resampy
numba
python_speech_features
ffmpeg-python
imageio-ffmpeg

# ─── Computer vision + face detection (Studio preprocess) ────────────
opencv-python-headless
mediapipe>=0.10.0
face_alignment

# ─── Numerical / ML core ─────────────────────────────────────────────
numpy>=1.24,<2.0
scipy
scikit-learn
pandas
tqdm
matplotlib

# ─── Misc utilities ───────────────────────────────────────────────────
configargparse
typeguard==2.13.3
ninja
packaging
rich
websockets>=12.0
websocket-client
onnxruntime-gpu
